import React, { useEffect } from "react";
import UnregisterPageComponent from "../../component/user/UnregisterPageComponent";
const UnregisterPageContainer = () => {
    useEffect(() => {}, []);
    const propDatas = {};
    return <UnregisterPageComponent {...propDatas} />;
};

export default UnregisterPageContainer;
